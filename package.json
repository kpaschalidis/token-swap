{
  "name": "token-migration-tool",
  "version": "1.0.0",
  "description": "Token migration tool",
  "license": "UNLICENSED",
  "scripts": {
    "postinstall": "run-s install:*",
    "install-all": "run-s install:*",
    "install:suite-ui": "cd applications/suite/ui && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "^12.14.1",
    "npm": "^6.13.4"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5"
  }
}
